{
    "name": "edu-js-creative",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "live-server --port=3000 --watch=. --entry-file=./index.html"
    },
    "devDependencies": {
        "live-server": "^1.2.0"
    }
}
